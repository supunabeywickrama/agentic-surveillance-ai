# Multi-Agent AI Surveillance System - Running Guide

Welcome to the Agentic Surveillance AI project! This document outlines step-by-step instructions on how to set up, activate, and run the various components of the intelligence surveillance pipeline.

## 🛠 Prerequisites

Ensure you have Python 3.10+ installed on your system.

## 📦 Step 1: Clone & Setup the Environment

We strongly recommend using a virtual environment to manage project dependencies.

```powershell
# 1. Create a virtual environment
python -m venv venv

# 2. Activate the virtual environment
# On Windows:
.\venv\Scripts\activate
# On Linux/macOS:
source venv/bin/activate

# 3. Install the dependencies
pip install -r requirements.txt
```

## 🧠 Step 2: Running the AI Agentic Pipeline

The core application engine extracts video frames, invokes the vision models for object tracking, flags behavioral events (like loitering or restricted zone entry), and uses a **LangGraph** flow of Agents to process the reasoning using a Vector Database (RAG) before issuing an alert.

```powershell
# Make sure your virtual environment is active!
python main.py
```
*Note: A dummy video and dummy detections are generated automatically to test the pipeline flow if actual models and camera feeds are not present.*

### What happens when you run `main.py`?
1. Extracts frames to `data/frames/`.
2. Initializes the `AdvancedTracker` (bounding boxes).
3. `EventDetector` Flags loitering and crowd detection triggers.
4. **LangGraph Workflow**: Vision Agent -> Context Agent (using RAG) -> Decision Agent -> Report & Alert Agents.
5. High-risk incidents are logged into a local `incidents.db` (SQLite) and Markdown files are exported to `data/processed/reports/`.

---

## 📡 Step 3: Running the Backend FastAPI Server

We expose an API representing the backend services where external dashboards and monitoring clients can query system health or incident records.

**Run the Server (using Uvicorn):**
```powershell
# Starts the server on localhost:8000
uvicorn backend.api:app --reload
```

**Testing the API:**
Open your browser and navigate to:
- Interactive API Docs: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
- System Status endpoint: [http://127.0.0.1:8000/status](http://127.0.0.1:8000/status)

---

## 📊 Step 4: Accessing the Outputs

- **Extracted Frames:** Look inside the `data/frames/` directory.
- **Incident Database Logs:** We use SQLite. Use an explorer tool (like DB Browser for SQLite) to open `incidents.db` and query the `incidents` table.
- **Detailed Incident Reports:** Look in `data/processed/reports/` for Auto-generated Markdown (`.md`) incident explanations generated by our Reporting Agent!

---

## 🚀 Advanced Deployment (Incoming Features)

Once Docker and the Streamlit UI dashboard are set up, this repository will support running the full stack at once via Docker Compose:
```bash
docker-compose up --build
```
*(This feature is currently marked as To-Do in our task checklist!)*
